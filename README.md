# Main Title - Git-Course Repo
## Major Section
### Subsection

## Experimenting with README.md 
### Paragraph
This is and example of first paragraph. Something, something, something. 

This repo is for something, something, something. It's intended use is for this, this and this but not for this or this. The text is very short in length and I have to increase the word count to make it look like a real paragraph rather than just some few line. 

A third para maybe. Why not? Seems fair.

### Font style

**This is Bold.** It's barely bolder. **Eh, on second thought maybe not.**

*This is in Italics.* Seems about right.

***Mama, mia!***

~~Strike this out!~~ That's done. You don't need to see it now.

### Code

Run this `functions.py` and then `scrips.py` to run the program.

```python
import matplot.pyplot as plt
import numpy as np

x = np.linspace(0,2*np.pi, 1000)

plt.plot(x, np.sin(x))
plt.show()
```
This is for powershell

```powershell
py -0p
where.exe python
.\.venv\Scripts\Activate.ps1
```

This is for bash
```bash
git init
git remote add origin <URL>
git add .
git commit -m 'initial commit'
git push -u origin main
```

This is for plain text
```text
#Include these in .gitignore

.venv\
__pycache__\
.ipynb_checkpoints\
```
### Lists
Simple lists
1. Style 1 with -
- one
- two
- three
2. Style 2 with *
* one
* two 
* three
No difference

Nested Lists
- one 
    - one one
    - one two
- two
    - two one
    - two two
    - two three
        - two three one
        - two three two
            - two three two one

Enough for toady. Will continue ***Tomorrow***

But before going added **Cosec Graph** to  `Ml_Notebook.ipynb` with *AI*. 
